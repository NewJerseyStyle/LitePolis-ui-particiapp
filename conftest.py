"""Pytest configuration for LitePolis-ui-particiapp tests."""

import pytest
import sys
from pathlib import Path

# Add the package to the path for testing
sys.path.insert(0, str(Path(__file__).parent))
